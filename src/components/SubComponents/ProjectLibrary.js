import React from 'react';
import styled from 'styled-components';
const Wrapper = styled.div`
  margin-top: 1em;
  margin-left: 180px;
  margin-right: 6em;
`;

export const ProjectLibrary = (props) => (
  <Wrapper>
    <h2>Project Library</h2>
  </Wrapper>
)